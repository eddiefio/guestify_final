{
  "name": "guestify",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd guestify-app && npm run dev",
    "build": "cd guestify-app && npm run build",
    "start": "cd guestify-app && npm run start",
    "lint": "cd guestify-app && npm run lint",
    "vercel-build": "cd guestify-app && npm install && npm run build"
  }
} 