
export const SCHEMA_NAME = {
  USERS: "users",
  PROFILES: "profiles",
  CHECKOUT_SESSIONS: "checkout_sessions",
  SUBSCRIPTIONS: "subscriptions",
};

export type SCHEMA_NAME_TYPE = keyof typeof SCHEMA_NAME;
