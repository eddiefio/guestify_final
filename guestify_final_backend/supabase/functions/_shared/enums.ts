export enum StripeCheckoutSessionStatus {
  ACTIVE = "ACTIVE",
  COMPLETED = "COMPLETED",
  EXPIRED = "EXPIRED",
  INACTIVE = "INACTIVE",
}

export enum SubscriptionPlan {
  MONTHLY = "monthly",
  YEARLY = "yearly",
}

export enum SubscriptionStatus {
  ACTIVE = "ACTIVE",
  PENDING = "PENDING",
  CANCELLED = "CANCELLED",
  PAUSED = "PAUSED",
  TRIALING = "TRIALING",
  UNPAID = "UNPAID",
}